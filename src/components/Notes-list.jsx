import React from "react";
import style from "../styles/NotesList.module.css";
import 

function NotesList({notesArray}){
    return (
        <section>
            <h2 className={style.heading}>Active Notes</h2>
            
        </section>
    );
}

export default NotesList;